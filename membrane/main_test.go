package membrane

import "testing"

// Test the start of the membrane server if we are missing a gateway plugin implementation
// Spoiler alert, the membrane server should exit
func TestMissingGatewayPlugin(t *testing.T) {
	t.Skip()
}
