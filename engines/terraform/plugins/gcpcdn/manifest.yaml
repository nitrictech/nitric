name: nitric-gcp-cdn
version: 0.0.1
type: entrypoint
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/HomelessDinosaur/nitric//engines/terraform/plugins/gcpcdn/module?depth=1&ref=gcp-cloud-run

inputs:

outputs:
