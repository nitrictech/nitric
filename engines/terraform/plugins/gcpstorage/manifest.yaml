name: nitric-gcp-storage
version: 0.0.1
type: storage
required_identities:
  - aws:iam:role
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/HomelessDinosaur/nitric//engines/terraform/plugins/gcpstorage/module?depth=1&ref=gcp-cloud-run
runtime:
  go_module: github.com/nitrictech/nitric/engines/terraform/plugins/gcpstorage

inputs:

outputs:
