name: nitric-gcp-cloud-run
version: 0.0.1
type: service
required_identities:
  - gcp:iam:role
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/HomelessDinosaur/nitric//engines/terraform/plugins/gcpcloudrun/module?depth=1&ref=gcp-cloud-run
runtime:
  go_module: github.com/nitrictech/nitric/engines/terraform/plugins/gcpcloudrun

inputs:

outputs:
