name: nitric-aws-lb
version: 0.0.1
type: infra
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/nitric//engines/terraform/plugins/awslb/module?depth=1&ref=next

inputs:

outputs:
