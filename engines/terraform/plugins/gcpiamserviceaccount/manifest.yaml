name: nitric-gcp-iam-service-account
version: 0.0.1
type: identity
identity_type: gcp:iam:service_account
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/HomelessDinosaur/nitric//engines/terraform/plugins/gcpiamserviceaccount/module?depth=1&ref=gcp-cloud-run

inputs:

outputs:
