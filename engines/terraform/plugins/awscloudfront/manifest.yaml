name: nitric-aws-cloudfront
version: 0.0.1
type: entrypoint
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/nitric//engines/terraform/plugins/awscloudfront/module?depth=1&ref=next

inputs:

outputs: