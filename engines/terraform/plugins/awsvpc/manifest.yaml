name: nitric-aws-vpc
version: 0.0.1
type: infra
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/tjholm/nitric//engines/terraform/plugins/awsvpc/module?depth=1&ref=feat/plugins

inputs:

outputs: