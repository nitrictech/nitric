name: nitric-aws-fargate
version: 0.0.1
type: service
required_identities:
  - aws:iam:role
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/nitric//engines/terraform/plugins/awsfargate/module?depth=1&ref=next
runtime:
  go_module: github.com/nitrictech/nitric/engines/terraform/plugins/awsfargate

inputs:

outputs:
