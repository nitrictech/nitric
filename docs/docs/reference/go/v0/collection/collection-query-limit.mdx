---
description: "Reference for Nitric's v0 Go library - Limit the number of results returned by a query."
---

# Go - Collection.Query.Limit()

Limit the number of results returned by a query.

```go
import (
  "fmt"

  "github.com/nitrictech/go-sdk/nitric"
)

func main() {
  profiles, err := nitric.NewCollection("profiles").With(nitric.CollectionReading, nitric.CollectionWriting, nitric.CollectionDeleting)
  if err != nil {
    return
  }

  profilesQuery := profiles.Query().Limit(10)

  nitric.Run()
}
```

## Parameters

<Properties>
  <Property name="limit" required type="int">
    The limit to apply to the query.
  </Property>
</Properties>

### See also

- [Query().FromPagingToken()](./collection-query-pagingfrom)
- [Query().Where()](./collection-query-where)
